% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{anova.mm_lmm}
\alias{anova.mm_lmm}
\title{Analysis of variance for a mixeff LMM}
\usage{
\method{anova}{mm_lmm}(
  object,
  ...,
  type = c("III", "II", "I", "block"),
  method = c("auto", "satterthwaite", "kenward_roger", "bootstrap", "asymptotic", "none"),
  refit_for_comparison = c("auto", "error", "ml")
)
}
\arguments{
\item{object}{A fitted \code{mm_lmm}.}

\item{...}{Optional additional fitted models; triggers \code{\link[=compare]{compare()}}.}

\item{type}{Term-hypothesis type. \code{"III"} (default) tests marginal
Type III hypotheses: a term's own contrast columns plus the
equally-weighted average over the levels of every term that contains
it, which makes the test invariant to which factor level is the
reference and matches SAS / \code{car} / \code{lmerTest} Type III. \code{"II"}
respects marginality (each term adjusted for all terms that do not
contain it). \code{"I"} is sequential in \code{terms()} order (main effects,
then two-way interactions, and so on). \code{"block"} tests the raw
coefficient block for each term — under treatment coding that is the
simple effect at the other factors' reference levels, which is a
legitimate quantity but is \emph{not} Type III on unbalanced designs; it
is the hypothesis mixeff computed for \code{"III"} before engine
\verb{1f3f689}.}

\item{method}{Degrees-of-freedom / test method.}

\item{refit_for_comparison}{Passed to \code{\link[=compare]{compare()}} when \code{...} is used.}
}
\value{
An \code{mm_anova} object.
}
\description{
With no extra models, \code{anova()} returns a term-level table for \code{object}.
With further fitted models in \code{...}, it defers to \code{\link[=compare]{compare()}} for a
nested model comparison.
}
