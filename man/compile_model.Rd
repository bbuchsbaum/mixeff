% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compile.R
\name{compile_model}
\alias{compile_model}
\title{Compile a mixed-effects model spec without fitting}
\usage{
compile_model(formula, data)
}
\arguments{
\item{formula}{A two-sided lme4-style formula, e.g.
\code{y ~ x + (1 + x | subject)}.}

\item{data}{A \code{data.frame} whose columns include every variable named
in \code{formula}. Variables with missing values raise an
\code{mm_data_error}; pass \code{na.omit(data)} explicitly if that is what you
want.}
}
\value{
An object inheriting from \code{mm_spec} and containing:
\describe{
\item{\code{call}}{the matched call}
\item{\code{formula}}{the input formula}
\item{\code{vars}}{character vector of variables read from \code{data}}
\item{\code{model_frame}}{the data columns used to compile the artifact,
retained so prefit audit views can evaluate nearby formula spellings}
\item{\code{artifact}}{parsed JSON artifact (the
\code{mixedmodels.compiled_model_artifact} v1 schema)}
}
The raw artifact JSON is attached as \code{attr(spec$artifact, "raw_json")}
so the post-compile FFI calls (e.g., the internal
\code{mm_audit_report_text} primitive) can round-trip without re-encoding.
}
\description{
\code{compile_model()} parses the formula, runs the upstream semantic-IR /
design-audit pipeline against the supplied data, and returns an
\code{mm_spec} object — the audit-first analogue of the design-only step in
base \code{lm()}'s \code{model.frame()} / \code{model.matrix()} chain. Nothing is
optimized; nothing is fitted. \code{audit_design()}, \code{explain_model()},
\code{random_options()}, and (in Phase 1.E) \code{lmm()} all consume the same
artifact.
}
\details{
The compiled artifact is the structured truth: every print, summary,
and audit verb in mixeff reads back from it rather than re-deriving
meaning from formula text. R formats; Rust authors wording (PRD §9.6).

Phase 1 compile scope: returns a populated \code{mm_spec} with the JSON
artifact attached. \code{explain_model()}, \code{random_options()}, and
\code{compare_covariance()} render random-effects guidance from upstream
random-term cards; the fit driver (\code{lmm()}) lands in 1.E.
}
\section{Errors}{

Raises typed conditions (all inheriting from \code{mm_condition}):
\itemize{
\item \code{mm_formula_error} — formula is not a two-sided R formula or
fails parsing.
\item \code{mm_data_error} — \code{data} is not a data.frame, refers to
unknown variables, contains NAs in design columns, or has an
unsupported column type.
\item \code{mm_schema_error} — the artifact JSON returned by Rust does
not match the wrapper's known schema set.
}
}

\examples{
\dontrun{
df <- data.frame(
  y       = rnorm(20),
  x       = rnorm(20),
  subject = factor(rep(letters[1:5], each = 4))
)
spec <- compile_model(y ~ x + (1 + x | subject), df)
audit_design(spec)
}

}
\seealso{
\code{\link[=audit_design]{audit_design()}} for the printed audit report.
}
