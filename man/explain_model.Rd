% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explain.R
\name{explain_model}
\alias{explain_model}
\title{Explain the random-effects structure of a compiled model}
\usage{
explain_model(spec)
}
\arguments{
\item{spec}{An \code{mm_spec} produced by \code{\link[=compile_model]{compile_model()}} or an \code{mm_fit}.}
}
\value{
An object of class \code{mm_explanation} carrying:
\describe{
\item{\code{text}}{the rendered explanation text}
\item{\code{cards}}{the upstream random-term cards}
\item{\code{cross_card_constraints}}{report-level constraints between cards}
\item{\code{diagnostics}}{the upstream diagnostics used for design notes}
\item{\code{report}}{the parsed upstream \code{ModelAuditReport}}
}
}
\description{
\code{explain_model()} renders the random-effects guidance surface for an
\code{mm_spec} returned by \code{\link[=compile_model]{compile_model()}} or, in later phases, an \code{mm_fit}.
It formats the upstream \code{RandomTermCard} and diagnostic payloads; Rust
remains the source of truth for per-block English wording and design facts.
}
\section{Errors}{

Raises an \code{mm_schema_error} if \code{spec} is not an \code{mm_spec}/\code{mm_fit} or does
not carry a valid compiled artifact.
}

\examples{
\dontrun{
df <- data.frame(
  y = rnorm(20),
  t = rep(0:3, 5),
  s = factor(rep(1:5, each = 4))
)
explain_model(compile_model(y ~ t + (1 | s), df))
}

}
\seealso{
\code{\link[=compile_model]{compile_model()}}, \code{\link[=audit]{audit()}}.
}
