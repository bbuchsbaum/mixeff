% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-lmm.R
\name{lmm}
\alias{lmm}
\title{Fit a linear mixed-effects model}
\usage{
lmm(
  formula,
  data,
  REML = TRUE,
  weights = NULL,
  subset = NULL,
  na.action = NULL,
  contrasts = NULL,
  control = mm_control()
)
}
\arguments{
\item{formula}{A two-sided lme4-style formula, e.g.
\code{y ~ x + (1 + x | subject)}.}

\item{data}{A \code{data.frame} containing all variables in \code{formula}.}

\item{REML}{Logical; fit by restricted maximum likelihood when \code{TRUE}.}

\item{weights}{Optional positive numeric case weights, either a vector with
one value per row or an expression evaluated in \code{data}.}

\item{subset}{Optional expression selecting rows of \code{data}, evaluated in
\code{data} (as in \code{\link[stats:lm]{stats::lm()}}).}

\item{na.action}{Optional function controlling missing-value handling,
applied to the model variables before fitting (e.g. \link[stats:na.fail]{stats::na.omit}).
The default (\code{NULL}) refuses any \code{NA} in a model variable with a typed
\code{mm_data_error} (audit-first: missing-data dropping must be opt-in). Pass
\code{na.action = na.omit} for lme4's complete-case behaviour.}

\item{contrasts}{Optional named list of factor contrasts. The engine codes
all factors with treatment contrasts; a request for any other coding is
refused (recode the factor instead).}

\item{control}{A list from \code{\link[=mm_control]{mm_control()}}.}
}
\value{
An object of class \code{mm_lmm}, also inheriting from \code{mm_fit} and
\code{mm_compiled}.
}
\description{
\code{lmm()} is mixeff's Phase 1 linear mixed-model fit driver. It compiles the
requested lme4-style formula, prints the same \code{\link[=explain_model]{explain_model()}} view that
pre-fit audit users see, then delegates the numerical fit to the upstream
Rust \code{LinearMixedModel}.
}
\details{
The returned object is deliberately serializable: fixed effects, theta,
sigma, likelihood summaries, fitted values, residuals, random effects, and
the post-fit compiler artifact are all stored directly on the R object. The
native Rust handle is treated as a rebuildable cache, not as the source of
truth.
}
\examples{
\dontrun{
df <- data.frame(
  y = rnorm(40),
  x = rep(0:3, 10),
  subject = factor(rep(seq_len(10), each = 4))
)
fit <- lmm(y ~ x + (1 + x | subject), df, control = mm_control(verbose = -1))
fixef(fit)
VarCorr(fit)
}

}
