% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broom.R
\name{mm_broom}
\alias{mm_broom}
\title{Tidy, glance, and augment methods for mixeff fits}
\arguments{
\item{x, data}{A fitted \code{mm_lmm} or \code{mm_glmm} (and, for \code{augment()}, optional
data to augment; defaults to the model frame).}

\item{effects}{Which terms to return: any of \code{"fixed"}, \code{"ran_pars"},
\code{"ran_vals"}.}

\item{conf.int}{Logical; add Wald \code{conf.low}/\code{conf.high} for fixed effects.}

\item{conf.level}{Confidence level for \code{conf.int}.}

\item{...}{Unused; for generic compatibility.}
}
\value{
A data frame.
}
\description{
These implement the \pkg{broom} / \pkg{broom.mixed} generics for
\code{\link[=lmm]{mm_lmm}} and \code{\link[=glmm]{mm_glmm}} fits, so \code{tidy()}, \code{glance()}, and
\code{augment()} work on mixeff models the same way they do on \code{lme4} fits.
}
\details{
\code{tidy()} returns one row per model term. \code{effects = "fixed"} yields the
fixed-effect coefficients (\code{estimate}, \code{std.error}, \code{statistic}, and, for
GLMMs, a Wald \code{p.value}); \code{effects = "ran_pars"} yields the variance-
component standard deviations (\verb{sd__<term>}), correlations
(\verb{cor__<a>.<b>}), and the residual SD (\code{sd__Observation}); \code{effects = "ran_vals"} yields the conditional modes. \code{glance()} returns a one-row
model-summary frame; \code{augment()} returns the model frame with \code{.fitted}
and \code{.resid} columns.

These methods are registered with \pkg{generics} when the package is loaded;
call them via \code{broom::tidy()} / \code{broom.mixed::tidy()} etc.
}
\keyword{internal}
