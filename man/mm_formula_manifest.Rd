% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{mm_formula_manifest}
\alias{mm_formula_manifest}
\title{The wrapper's formula manifest}
\usage{
mm_formula_manifest()
}
\value{
A named list with the following elements:
\describe{
\item{\code{mixeff_rust_version}}{Version of the bundled extendr crate.}
\item{\code{crate_version}}{Version of the bundled \code{mixeff-rs} upstream
crate.}
\item{\code{schema_versions}}{Named list, one entry per artifact schema
the wrapper currently emits or consumes.}
\item{\code{formula_features}}{Named list with \code{operators},
\code{intercept_forms}, \code{random_term_forms}, and \code{transformations} —
the lme4-style syntax surface.}
\item{\code{capabilities}}{Named list of logical flags
(\code{parse_formula}, \code{compile_model}, \code{audit_design}, \code{explain_model},
\code{random_options}, \code{compare_covariance}, \code{fit_lmm}, \code{fit_glmm},
\code{audit}, \code{changes}, \code{diagnostics}, \code{fit_status},
\code{parameterization}, \code{roles}, \code{as_json}, \code{simulate}, \code{inference},
\code{model_comparison_table}, \code{fit_summary_payload},
\code{marginal_quantity_table},
\code{marginal_quantities}, \code{verify_convergence}). The
\code{marginal_quantity_table} schema may be available before the
corresponding \code{marginal_quantities} verbs are implemented.}
}
}
\description{
A versioned record of what \code{mixeff} currently supports — formula syntax
surface, schema versions per artifact type, and capability flags. The
manifest is the wrapper's machine-readable answer to \emph{"what does this
build know how to do?"}. Fitted objects do not carry the whole manifest:
each one stamps the narrower \code{fit$schema} record (artifact schema name and
version, crate version, package version), which the serialization and
reporting paths read back. What lets \code{\link[=audit]{audit()}} describe a fit after
\code{readRDS()}, with no live Rust handle, is the durable compiled artifact
stored beside it.
}
\details{
Capability flags evolve across releases; gate behavior on flags rather than
on package version.
}
\examples{
m <- mm_formula_manifest()
m$schema_versions$formula
m$capabilities$parse_formula

}
