% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal.R
\name{mm_grid}
\alias{mm_grid}
\alias{mm_grid.mm_lmm}
\alias{mm_predictions}
\alias{mm_predictions.mm_lmm}
\alias{mm_means}
\alias{mm_means.mm_lmm}
\alias{mm_comparisons}
\alias{mm_comparisons.mm_lmm}
\title{Marginal grids, predictions, means, and comparisons}
\usage{
mm_grid(fit, specs, by = NULL, at = list(), cov.reduce = mean, ...)

\method{mm_grid}{mm_lmm}(fit, specs, by = NULL, at = list(), cov.reduce = mean, ...)

mm_predictions(
  fit,
  grid = NULL,
  specs = NULL,
  by = NULL,
  at = list(),
  method = c("auto", "satterthwaite", "kenward_roger", "bootstrap", "asymptotic", "none"),
  level = 0.95,
  target = c("population"),
  scale = c("response", "link"),
  ...
)

\method{mm_predictions}{mm_lmm}(
  fit,
  grid = NULL,
  specs = NULL,
  by = NULL,
  at = list(),
  method = c("auto", "satterthwaite", "kenward_roger", "bootstrap", "asymptotic", "none"),
  level = 0.95,
  target = c("population"),
  scale = c("response", "link"),
  ...
)

mm_means(
  fit,
  specs,
  by = NULL,
  at = list(),
  grid = NULL,
  method = c("auto", "satterthwaite", "kenward_roger", "bootstrap", "asymptotic", "none"),
  level = 0.95,
  weights = c("equal", "proportional"),
  target = c("population"),
  scale = c("response", "link"),
  ...
)

\method{mm_means}{mm_lmm}(
  fit,
  specs,
  by = NULL,
  at = list(),
  grid = NULL,
  method = c("auto", "satterthwaite", "kenward_roger", "bootstrap", "asymptotic", "none"),
  level = 0.95,
  weights = c("equal", "proportional"),
  target = c("population"),
  scale = c("response", "link"),
  ...
)

mm_comparisons(
  fit,
  specs,
  by = NULL,
  at = list(),
  grid = NULL,
  comparison = c("difference", "ratio", "odds_ratio"),
  method = c("auto", "satterthwaite", "kenward_roger", "bootstrap", "asymptotic", "none"),
  level = 0.95,
  weights = c("equal", "proportional"),
  target = c("population"),
  scale = c("response", "link"),
  ...
)

\method{mm_comparisons}{mm_lmm}(
  fit,
  specs,
  by = NULL,
  at = list(),
  grid = NULL,
  comparison = c("difference", "ratio", "odds_ratio"),
  method = c("auto", "satterthwaite", "kenward_roger", "bootstrap", "asymptotic", "none"),
  level = 0.95,
  weights = c("equal", "proportional"),
  target = c("population"),
  scale = c("response", "link"),
  ...
)
}
\arguments{
\item{fit}{A fitted \code{mm_lmm}.}

\item{specs}{Character vector, or a one-sided formula such as \code{~ trt} or
\code{~ trt | group}, naming the displayed marginal dimensions.}

\item{by}{Optional character vector of grouping variables for marginal
summaries or pairwise comparisons.}

\item{at}{Named list of fixed-predictor values to force in the grid.}

\item{cov.reduce}{Function used to reduce numeric fixed predictors that are
not explicitly gridded.}

\item{...}{Reserved for future methods.}

\item{grid}{Optional object returned by \code{mm_grid()}.}

\item{method}{Requested inference method, passed to \code{\link[=contrast]{contrast()}}.}

\item{level}{Confidence level for intervals computed from contrast standard
errors.}

\item{target}{Prediction target. Only \code{"population"} is implemented.}

\item{scale}{Prediction scale. Gaussian LMMs have identical \code{"link"} and
\code{"response"} scales.}

\item{weights}{Averaging weights for \code{mm_means()} and \code{mm_comparisons()}.
\code{"equal"} weights reference-grid cells equally; \code{"proportional"} weights
cells by observed fixed-factor frequencies.}

\item{comparison}{Comparison scale. Only \code{"difference"} is implemented.}
}
\value{
\code{mm_grid()} returns an \code{mm_grid} object. The other helpers return an
\code{mm_marginal_quantity} object with a contract-shaped \code{table}.
}
\description{
These helpers provide a small native marginal-quantities surface for
Gaussian LMM fits. They cover the common population-level workflow:
construct a reference grid, evaluate fixed-effect predictions, average them
into marginal means, and compare those means by simple differences.
}
\details{
The returned tables use the \code{mixedmodels.marginal_quantity_table} row
contract. Inference is routed through \code{\link[=contrast]{contrast()}} so rows retain the same
method, status, reliability, estimability, and reason fields as fixed-effect
contrasts. Ordinary full-rank LMMs use the versioned
\code{mixedmodels.fixed_effect_covariance_matrix} payload for fixed-effect
uncertainty; rank-deficient or otherwise uncertified fits surface explicit
unavailable status and reasons instead of partial covariance numbers.
}
