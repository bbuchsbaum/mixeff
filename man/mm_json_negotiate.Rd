% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{mm_json_negotiate}
\alias{mm_json_negotiate}
\title{Negotiate a JSON schema header against what \code{mixeff} supports}
\usage{
mm_json_negotiate(header)
}
\arguments{
\item{header}{A list with at least \code{schema_name} and \code{schema_version}
as length-1 character strings. Additional fields (e.g.,
\code{crate_version}, \code{package_version}) are accepted and ignored at this
layer; downstream code records them on the \code{mm_fit} provenance.}
}
\value{
Invisibly returns \code{TRUE} on a clean match.
}
\description{
Every artifact crossing the Rust-R bridge carries a header that names a
schema (e.g., \code{formula}, \code{artifact}, \code{audit}) and the version of that
schema (\code{v0}, \code{v1}, ...). \code{mm_json_negotiate()} validates a header
against the closed set of \verb{(schema_name, schema_version)} pairs the
current wrapper build understands, and raises a typed
\code{mm_schema_error} on mismatch.
}
\details{
This is the \emph{fast-fail} primitive: any code path that consumes a Rust
artifact should call \code{mm_json_negotiate()} before parsing the body, so
a version skew between the Rust crate and the R wrapper produces a
single clean error rather than a confusing field-by-field decode failure.
}
\section{Errors}{

Any of the following raise an \code{mm_schema_error} (also inheriting from
\code{mm_condition} and \code{error}):
\itemize{
\item \code{header} is not a list, or is missing \code{schema_name} /
\code{schema_version}, or those fields are not length-1 character.
\item \code{schema_name} is not in the wrapper's known set
(see \code{\link[=mm_json_known_schemas]{mm_json_known_schemas()}}).
\item \code{schema_name} is known but \code{schema_version} does not match what
the wrapper expects.
}
The condition object carries the original \code{header} in its \code{input} field.
(The field is \emph{not} called \code{header} because rlang reserves that name on
conditions for \code{cnd_header()} formatting.)
}

\examples{
mm_json_negotiate(list(schema_name = "formula", schema_version = "v0"))
\dontrun{
# Raises mm_schema_error:
mm_json_negotiate(list(schema_name = "formula", schema_version = "v99"))
}

}
\seealso{
\code{\link[=mm_json_known_schemas]{mm_json_known_schemas()}} for the closed set,
\code{\link[=mm_formula_manifest]{mm_formula_manifest()}} for the broader capability record.
}
