% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{mm_lincomb}
\alias{mm_lincomb}
\alias{mm_lincomb.default}
\alias{mm_lincomb.mm_glmm}
\alias{mm_lincomb.mm_lmm}
\title{Wald inference on a linear combination of fixed effects}
\usage{
mm_lincomb(fit, weights, level = 0.95, method = NULL, ...)

\method{mm_lincomb}{default}(fit, weights, level = 0.95, method = NULL, ...)

\method{mm_lincomb}{mm_glmm}(fit, weights, level = 0.95, method = NULL, ...)

\method{mm_lincomb}{mm_lmm}(
  fit,
  weights,
  level = 0.95,
  method = c("auto", "satterthwaite", "kenward_roger", "asymptotic"),
  ...
)
}
\arguments{
\item{fit}{A fitted \code{mm_lmm} or \code{mm_glmm}.}

\item{weights}{A named numeric vector (or named list / single-row
data.frame coercible to one). Names must match
\code{names(fixef(fit))} exactly.}

\item{level}{Confidence level for the Wald interval. Default 0.95.}

\item{method}{For \code{mm_lmm}, the degrees-of-freedom method passed to
\code{\link[=df_for_contrast]{df_for_contrast()}}. Defaults to \code{"auto"} (Satterthwaite when
available). For \code{mm_glmm}, only \code{"asymptotic"} is accepted.}

\item{...}{Reserved for future methods.}
}
\value{
A single-row data.frame with columns \code{estimate}, \code{std_error},
\code{statistic}, \code{statistic_name} (\code{"t"} or \code{"z"}), \code{df}, \code{p_value},
\code{lower}, \code{upper}, and \code{method}. The result carries an \code{"mm_status"}
attribute reflecting the underlying vcov reliability (\code{status},
\code{method}, \code{reliability}, \code{reason}).
}
\description{
Convenience helper for the common case of testing
\eqn{H_0:\, c^\top \beta = 0}{H0: c' beta = 0} where \code{c} is a sparse,
named weight vector. The estimate is \eqn{c^\top \hat\beta}{c' beta_hat},
the standard error is \eqn{\sqrt{c^\top V c}}{sqrt(c' V c)} where \code{V}
is the model's fixed-effect covariance, the statistic is the Wald
ratio, and the interval is the symmetric Wald CI at \code{level}.
}
\details{
For \code{mm_glmm}, the statistic is the asymptotic Wald \emph{z} (no df). For
\code{mm_lmm}, the default is Satterthwaite-approximated \emph{t} via
\code{\link[=df_for_contrast]{df_for_contrast()}}; pass \code{method = "asymptotic"} to force Wald \emph{z}.

Weight names must be a subset of \code{names(fixef(fit))}. Coefficients
not named in \code{weights} contribute zero. Pass the long-form
\code{\link[=contrast]{contrast()}} front door if you need multiple contrasts or non-default
rhs.
}
\examples{
\dontrun{
# Difference-in-differences contrast at a focal SOA = 25 ms
# (Loo et al. 2026 aphantasia primary estimand, glmm path)
soa_s_25 <- (log(0.025) - mean(fit$data$soa_log)) / sd(fit$data$soa_log)
mm_lincomb(fit, c(
  "group: aphant:mask: masked"        = 1,
  "group: aphant:mask: masked:soa_s"  = soa_s_25
))
}

}
\seealso{
\code{\link[=contrast]{contrast()}} for the long-form, Rust-routed contrast surface
with full estimability / reliability reporting.
}
