% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmm.R
\name{mm_negative_binomial}
\alias{mm_negative_binomial}
\title{Negative-binomial family for \code{glmm()}}
\usage{
mm_negative_binomial(theta = NULL)
}
\arguments{
\item{theta}{Optional positive NB2 size (dispersion) parameter. \code{NULL}
estimates theta from the data.}
}
\value{
A \code{family} object accepted by \code{\link[=glmm]{glmm()}}.
}
\description{
NB2 family (variance \code{mu + mu^2/theta}, log link). With \code{theta = NULL}
(the default) the size parameter is estimated alongside the model, matching
\code{lme4::glmer.nb()}. Supplying a positive \code{theta} fits conditional on that
value, matching \code{glmer(family = MASS::negative.binomial(theta))} — which
\code{glmm()} also accepts directly.
}
\examples{
fam <- mm_negative_binomial()      # glmer.nb-style: theta estimated
fam_fixed <- mm_negative_binomial(theta = 2.5)
}
