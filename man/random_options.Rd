% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/random-options.R
\name{random_options}
\alias{random_options}
\title{Inspect nearby random-effect spellings for one grouping factor}
\usage{
random_options(spec, group, slope = NULL)
}
\arguments{
\item{spec}{An \code{mm_spec} from \code{\link[=compile_model]{compile_model()}} or, in later phases, an
\code{mm_fit}.}

\item{group}{Grouping factor to inspect. May be supplied bare
(\code{group = subject}) or as a string.}

\item{slope}{Optional slope variable to use for nearby slope-bearing
spellings. When omitted, the function uses the first current random
slope for \code{group}, then any scope-note fixed effect for \code{group}, then
the first non-intercept fixed effect.}
}
\value{
An object of class \code{mm_random_options} with an \code{options} data frame,
the upstream candidate \code{cards}, and the candidate audit reports.
}
\description{
\code{random_options()} is an opt-in map over nearby random-effect structures
for a grouping factor. It recompiles each displayed spelling through the
same upstream audit path as \code{\link[=compile_model]{compile_model()}}, so support facts and block
meanings come from Rust-authored \code{RandomTermCard} records.
}
