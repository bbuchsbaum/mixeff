% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{test_random_effect}
\alias{test_random_effect}
\alias{test_random_effect.mm_lmm}
\title{Test a random-effect variance component}
\usage{
test_random_effect(
  fit,
  term,
  method = c("boundary_lrt"),
  refit_for_comparison = c("auto", "error", "ml"),
  ...
)

\method{test_random_effect}{mm_lmm}(
  fit,
  term,
  method = c("boundary_lrt"),
  refit_for_comparison = c("auto", "error", "ml"),
  ...
)
}
\arguments{
\item{fit}{A fitted \code{mm_lmm}.}

\item{term}{Random-effect term to test. This can be the term id (\code{"r0"}),
the original random-effect fragment such as \code{"(1 | subject)"}, or a
unique grouping factor name such as \code{"subject"}.}

\item{method}{Currently \code{"boundary_lrt"}.}

\item{refit_for_comparison}{How to handle REML fits. \code{"auto"} and \code{"ml"}
refit to ML; \code{"error"} refuses.}

\item{...}{Reserved for future methods.}
}
\value{
An \code{mm_random_effect_test} object with a one-row \code{table}.
}
\description{
\code{test_random_effect()} exposes the boundary-aware likelihood-ratio route for
random-effect variance components. The v1 certified route is a nested ML
comparison that adds exactly one variance/covariance parameter and reports
the Self-Liang 50:50 mixture reference distribution. It is intentionally
separate from \code{\link[=test_effect]{test_effect()}}, which tests fixed effects.
}
