% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/update.R
\name{update.mm}
\alias{update.mm}
\alias{update.mm_lmm}
\alias{update.mm_glmm}
\title{Update and re-fit a mixeff model}
\usage{
\method{update}{mm_lmm}(object, formula., ..., evaluate = TRUE)

\method{update}{mm_glmm}(object, formula., ..., evaluate = TRUE)
}
\arguments{
\item{object}{A fitted \code{mm_lmm} or \code{mm_glmm}.}

\item{formula.}{A formula-change applied with \code{\link[stats:update.formula]{stats::update.formula()}};
omit to keep the current formula. Random-effect terms (\code{(x | g)},
\code{(x || g)}) are preserved across \code{. ~ .} edits.}

\item{...}{Arguments to override on the re-fit. For \code{mm_lmm}: \code{data},
\code{REML}, \code{weights}, \code{control}. For \code{mm_glmm}: additionally \code{family},
\code{offset}, \code{method}, \code{nAGQ}, \code{inference}.}

\item{evaluate}{If \code{TRUE} (default) re-fit and return the new model; if
\code{FALSE} return the unevaluated call.}
}
\value{
A new fitted model of the same class as \code{object}, or an unevaluated
call when \code{evaluate = FALSE}.
}
\description{
\code{update()} re-fits an \code{\link[=lmm]{mm_lmm}} or \code{\link[=glmm]{mm_glmm}} with a modified
model specification, mirroring \code{\link[stats:update]{stats::update()}} and \code{lme4}'s \code{update()} for
the common cases: changing the formula (\code{. ~ . - x}), toggling \code{REML},
swapping \code{weights}/\code{offset}/\code{family}/\code{control}, or supplying new \code{data}.
}
\details{
The re-fit reuses the fitted model frame (\code{\link[=model.frame]{model.frame()}}) as the default
data source, so formula edits that \emph{remove} terms or change estimation
options work without re-supplying data. A formula edit that introduces a
\strong{new} variable absent from the original model frame requires an explicit
\verb{data =} argument.
}
\examples{
\dontrun{
fit <- lmm(Reaction ~ Days + (Days | Subject), lme4::sleepstudy,
           control = mm_control(verbose = -1))
# drop the fixed slope, keep the random slope
fit2 <- update(fit, . ~ . - Days)
# refit by ML for a likelihood-ratio comparison
fit_ml <- update(fit, REML = FALSE)
}

}
