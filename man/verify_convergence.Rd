% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verify-convergence.R
\name{verify_convergence}
\alias{verify_convergence}
\alias{verify_convergence.default}
\alias{verify_convergence.mm_lmm}
\title{Verify convergence of a fitted linear mixed model}
\usage{
verify_convergence(fit, ...)

\method{verify_convergence}{default}(fit, ...)

\method{verify_convergence}{mm_lmm}(
  fit,
  ...,
  restart = TRUE,
  jitter_starts = 1L,
  jitter_scale = 1e-04,
  consensus = FALSE,
  max_feval = 500L,
  objective_tolerance = 1e-05,
  theta_tolerance = 0.001,
  beta_tolerance = 1e-04
)
}
\arguments{
\item{fit}{A fitted \code{mm_lmm} from \code{\link[=lmm]{lmm()}}.}

\item{...}{Reserved for future methods.}

\item{restart}{Logical; re-optimize starting from the fitted optimum and
compare against it.}

\item{jitter_starts}{Number of restarts from jittered copies of the fitted
covariance parameters.}

\item{jitter_scale}{Relative scale of the jitter applied to theta.}

\item{consensus}{Logical; also refit with an engine-chosen alternate
optimizer and compare. Default \code{FALSE}: this vendored build compiles
without the optional \code{nlopt} backend, and for some models the engine's
alternate choice is an nlopt optimizer — its absence would then be
reported as a non-agreeing run (status \code{fragile}) that reflects the
build, not the fit. Enable it when you want the consensus pass and will
read the per-run diagnostics.}

\item{max_feval}{Positive integer cap on objective evaluations per
verification run.}

\item{objective_tolerance, theta_tolerance, beta_tolerance}{Positive
agreement tolerances on the objective value, the covariance parameters,
and the fixed effects.}
}
\value{
An object of class \code{mm_convergence_verification} carrying:
\describe{
\item{\code{status}}{the engine verdict: \code{not_run}, \code{restart_agrees},
\code{optimizer_consensus}, \code{fragile}, or \code{unstable}}
\item{\code{message}}{the engine's one-line summary}
\item{\code{table}}{a data frame with one row per verification run (label,
optimizer, return code, objective/theta/beta deltas, agreement)}
\item{\code{reference}}{the reference optimum the runs were compared to}
\item{\code{tolerances}}{the agreement tolerances that were applied}
\item{\code{raw}}{the parsed engine payload}
}
}
\description{
\code{verify_convergence()} re-runs the fit under the engine's bounded
verification workflow and reports whether the extra runs agree with the
fitted optimum: a restart from the optimum, one or more jittered restarts,
and (opt-in) an alternate-optimizer consensus pass. Agreement is judged by
the engine against the objective/theta/beta tolerances below; the verdict
(\code{status}), the per-run deltas, and the wording are all owned by the Rust
contract — R only formats them.
}
\details{
The verifier refits the model from the stored specification before it
starts, so a call costs roughly \code{2 + jitter_starts} fits (plus consensus
runs when enabled).
}
\examples{
\dontrun{
fit <- lmm(y ~ t + (1 | s), df)
verify_convergence(fit)
}

}
\seealso{
\code{\link[=optimizer_certificate]{optimizer_certificate()}} for what the original fit ran;
\code{\link[=mm_control]{mm_control()}} to refit with a different optimizer or tolerances.
}
